N is assumed.

Z is assumed.

S is assumed.

plus is assumed.

plus_Z is assumed.

plus_S is assumed.

Type
      : Type
Typing error (./beta-nat.m31:12:5-21):
  this expression (refl (S (S Z))) should have type plus (S (S Z)) Z
                                                      == S (S Z) but has type 
  S (S Z)
    == S (S Z)
