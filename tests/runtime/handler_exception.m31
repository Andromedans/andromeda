exception Cow of mlstring ;;
operation moo : mlunit -> mlstring ;;

handle
  try
     let x = moo () in
     ("operation", x)
  with
  | Cow c -> ("correct", c)
  end
with
| moo () -> raise (Cow "moo")
| raise (Cow c) -> ("wrong", c)
end
