exception Cow of mlstring ;;
operation moo : mlunit -> mlstring ;;

try
  handle
    try
      let x = moo () in
      ("operation", x)
    with
    | Cow c -> ("correct", c)
    end
  with
  | moo () -> raise (Cow "moo")
  end
with
| Cow c -> ("wrong", c)
end