let a = "a" in
   match "b" with
   | x when mlfalse => "problem"
   | _ => a
   end
;;

match [[]] with
| s when (match s with [[]] => mltrue | _ => mlfalse end) => "yes"
| _ => "no"
end
;;

match [] with
| s when (match s with [[]] => mltrue | _ => mlfalse end) => "yes"
| _ => "no"
end
