unit is defined.
Constant top is declared.
Constant unit_rect is declared.
Constant unit_iota_top is declared.
it is defined.
⊢ refl top
  : (λ (T : Type), unit_rect (λ (m : {}), T → T) (λ (x : T), x))
      {} top top
      ≡ top
⊢ refl top
  : top
      ≡ (λ (T : Type), unit_rect (λ (m : {}), T → T) (λ (x : T), x))
            {} top top
⊢ refl top
  : top
      ≡ (λ (T : Type), unit_rect (λ (m : {}), T → T) (λ (x : T), x))
            {} top top
⊢ refl top
  : top
      ≡ (λ (T : Type), unit_rect (λ (m : {}), T → T) (λ (x : T), x))
            {} top top
⊢ refl top
  : top
      ≡ (λ (T : Type), unit_rect (λ (m : {}), T → T) (λ (x : T), x))
            {} top top
