constant A : Type
constant B : Type
constant P : A -> Type
constant Q : ∏ (a : A), P a → Type

check with equality_in betas hints handle
   assume b : B in
   assume e : B ≡ A in
   let _ = beta e in
   assume x : P b in
   assume f : Q b x → A in
     f

