(* Test an eta hint *)
constant unit : Type
constant top : unit
constant unit_eta : Π (x y : unit), x == y

check with equality_in betas hints handle
  let _ = hint unit_eta in
    λ (x : unit), (refl top : x == top)

