(* Eventually this example should not work. *)
(* We convince Andromeda that a has type B. *)
constant A : Type
constant B : Type
constant a : A

operation mafia 1

check
  (handle
     (λ (e : B == A), let crime = (with equality_in hints betas handle let _ = hint e in (a : B)) in mafia crime)
   with
   | mafia ?x => x
   end
  )

