(* Eventually this example should not work. *)
(* We convince Andromeda that a has type B. *)
constant A : Type
constant B : Type
constant a : A

operation mafia 1

do
  (handle
     (λ (e : B == A), let crime = (now hints = add_hint e hints in (a : B)) in mafia crime)
   with
   | mafia ?x => x
   end
  )

