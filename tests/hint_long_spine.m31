(* Test long spines against short patterns. *)

constant A : Type
constant B : Type
constant a : A
constant b : B
constant f : A -> B

constant f_def : f == (λ (x : A), b)

check
  with local (lbeta f_def) handle
    refl b : f a == b
