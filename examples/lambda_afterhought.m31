Parameter A : Type.
Parameter B : A -> Type.

Parameter pair : Π [x : A] B x → Type.

Check
   handle
     let a := #fresh A in
     let b := #fresh (B a) in
       λ [y : A] pair a b
   with
     | #fresh t k => λ [x : t] k x
   end.
